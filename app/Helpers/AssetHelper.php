<?php

namespace App\Helpers;

/**
 * Minimal AssetHelper stub to satisfy Composer autoload.
 *
 * If your application expects more functionality, replace this
 * with the original implementation.
 */
class AssetHelper
{
    /**
     * Return a simple asset path. Update as needed.
     */
    public static function url(string $path): string
    {
        return $path;
    }
}
